import React from 'react';

const Header = () => {
  return (
    <header>
      <div className="logo"></div>
      <nav>
        <a href="#">Home</a>
        <a href="#">Rotas</a>
        <a href="#">Comprar</a>
        <a href="#">Contato</a>
        <button>Entrar</button>
      </nav>
    </header>

  );
};

export default Header;
